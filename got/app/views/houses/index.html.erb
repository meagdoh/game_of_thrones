<h1>GoT Houses</h1>

<% @houses.each do |house| %>
  <div class = "house">
    <h2>
      <%= link_to house.name, house_path(house) %>
    </h2>
    <img src="<%= house.img_url %>" alt="<%= house.name %>">
  </div>

<% end %>
